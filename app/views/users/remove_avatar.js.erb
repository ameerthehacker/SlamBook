<% if current_user.errors.any? %>
    $("#avatar-update-info").html("<%= j render 'shared/errors', :obj => current_user %>");    
<% else %>
    $("#avatar-update-info").html("<%= j render 'shared/flash_message' %>");
    $('#img-avatar').attr('src', "<%= image_url('default-user-avatar.jpg') %>");
    $('#current-avatar').html("<%= j current_user.decorate.avatar_image(150) %>");    
<% end %>